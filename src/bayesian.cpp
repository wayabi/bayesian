#include "bayesian.h"
#include <iostream>

using namespace std;

gaussian bayesian::posterior_simple0(const gaussian& g, vec x, vec speed_elastic, vec speed_transition)
{
	return gaussian();
}

gaussian bayesian::posterior_outlier0(const gaussian& g, vec x, vec speed_elastic, vec speed_transition, double outlier_percentage)
{

}

